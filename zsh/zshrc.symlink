DOTFILES="$HOME/.dotfiles"

# Homebrew
[ -x /opt/homebrew/bin/brew ] && eval "$(/opt/homebrew/bin/brew shellenv)"

export PATH="$HOME/.local/bin:$PATH"
export GPG_TTY=$(tty)

[ -f "$DOTFILES/_local/zshrc" ] && source "$DOTFILES/_local/zshrc"

